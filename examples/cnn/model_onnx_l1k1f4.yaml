model:
  name: simple_cnn_onnx
  label: ONNX 1x4x1
  version: "1.0.0"
  inference_engine: onnx
  file: ./onnx_graph_l1k1f4.onnx
  inputs:
    - name: input
      shape: [28, 28, 1]
  outputs:
    - name: output
